<%= form_with(model: zone_i) do |form| %>
  <% if zone_i.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone_i.errors.count, "error") %> prohibited this zone_i from being saved:</h2>

      <ul>
        <% zone_i.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :desk_id, style: "display: block" %>
    <%= form.text_field :desk_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.label :campaign, style: "display: block" %>
    <%= form.text_field :campaign %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
