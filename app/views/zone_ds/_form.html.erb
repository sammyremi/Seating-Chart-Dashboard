<%= form_with(model: zone_d) do |form| %>
  <% if zone_d.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone_d.errors.count, "error") %> prohibited this zone_d from being saved:</h2>

      <ul>
        <% zone_d.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :desk_id, style: "display: block" %>
    <%= form.number_field :desk_id %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.label :campaign, style: "display: block" %>
    <%= form.text_field :campaign %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
